apiVersion: v1
kind: Service
metadata:
  name: service-a-svc
  labels:
    app: service-a
    service: service-a
spec:
  ports:
  - port: 80
    targetPort: 8080
    name: http-port
    protocol: TCP
  selector:
    app: service-a
